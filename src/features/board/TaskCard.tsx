import { memo } from 'react';
import { useSortable } from '@dnd-kit/sortable';
import { CSS } from '@dnd-kit/utilities';
import type { Task, User } from '../../types';
const priorityStyle={high:'bg-rose-100 text-rose-700',medium:'bg-amber-100 text-amber-700',low:'bg-emerald-100 text-emerald-700'};
export const TaskCard=memo(function TaskCard({task,user,onOpen}:{task:Task;user?:User;onOpen:(task:Task)=>void}){const{attributes,listeners,setNodeRef,transform,transition,isDragging}=useSortable({id:task.id});return <article ref={setNodeRef} style={{transform:CSS.Transform.toString(transform),transition}} className={`rounded-xl border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-900 ${isDragging?'opacity-50':''}`}><button className="w-full text-left" onClick={()=>onOpen(task)}><div className="mb-3 flex items-start justify-between gap-2"><span className={`rounded-full px-2 py-1 text-[11px] font-bold capitalize ${priorityStyle[task.priority]}`}>{task.priority}</span><span className="text-xs text-slate-400">#{task.id}</span></div><h3 className="font-semibold text-slate-900 dark:text-white">{task.title}</h3><p className="mt-2 line-clamp-2 text-xs text-slate-500">{task.description}</p></button><div className="mt-4 flex items-center justify-between"><div className="flex items-center gap-2">{user&&<img src={user.avatar} alt={`${user.name} avatar`} className="h-7 w-7 rounded-full" loading="lazy"/>}<span className="max-w-24 truncate text-xs text-slate-500">{user?.name}</span></div><span className="text-xs text-slate-500">{new Date(task.dueDate).toLocaleDateString(undefined,{month:'short',day:'numeric'})}</span></div><button {...attributes} {...listeners} className="mt-3 w-full cursor-grab rounded-lg border border-dashed border-slate-300 py-1.5 text-xs text-slate-500 active:cursor-grabbing dark:border-slate-700" aria-label={`Drag ${task.title}`}>Drag</button></article>});
