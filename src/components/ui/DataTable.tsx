import type { ReactNode } from 'react';
export type Column<T> = { key: string; header: string; render: (row: T) => ReactNode };
export function DataTable<T>({ rows, columns, getKey }: { rows: T[]; columns: Column<T>[]; getKey: (row: T) => string | number }) { return <div className="overflow-x-auto rounded-xl border border-slate-200 dark:border-slate-800"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500 dark:bg-slate-900"><tr>{columns.map((c)=><th key={c.key} className="px-4 py-3">{c.header}</th>)}</tr></thead><tbody className="divide-y divide-slate-200 bg-white dark:divide-slate-800 dark:bg-slate-950">{rows.map((row)=><tr key={getKey(row)}>{columns.map((c)=><td key={c.key} className="px-4 py-3 text-slate-700 dark:text-slate-200">{c.render(row)}</td>)}</tr>)}</tbody></table></div>; }
